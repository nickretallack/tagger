// Generated by CoffeeScript 1.7.1
(function() {
  var to_selectize_options;

  to_selectize_options = function(items) {
    var item, _i, _len, _results;
    _results = [];
    for (_i = 0, _len = items.length; _i < _len; _i++) {
      item = items[_i];
      _results.push({
        text: item,
        value: item
      });
    }
    return _results;
  };

  $(function() {
    var character_options, combo_options, tag_options;
    tag_options = to_selectize_options(TAG_NAMES);
    $('.tag-field').selectize({
      delimiter: ',',
      persist: false,
      create: function(input) {
        return {
          value: input,
          text: input
        };
      },
      options: tag_options
    });
    character_options = to_selectize_options(CHARACTER_NAMES);
    $('.character-field').selectize({
      delimiter: ',',
      persist: false,
      create: function(input) {
        return {
          value: input,
          text: input
        };
      },
      options: character_options
    });
    combo_options = tag_options.concat(character_options);
    return $('.tag-character-field').selectize({
      delimiter: ',',
      persist: false,
      create: function(input) {
        return {
          value: input,
          text: input
        };
      },
      options: combo_options
    });
  });

}).call(this);
